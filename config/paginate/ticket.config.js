export const ticketPaginateConfig = {
  sortableColumns: [],
  searchableColumns: [],
  filterableColumns: {
    order_id: true,
    ticket_price_id: true,
    showtime_seat_id: true,
  },
  defaultSortBy: [["id", "DESC"]],
  defaultLimit: 10,
  maxLimit: 50,
};
